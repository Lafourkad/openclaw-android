{
  "name": "memory-palace-android",
  "version": "1.0.0",
  "description": "OpenClaw plugin — on-device Memory Palace for Android (Room + HNSW + EmbeddingGemma)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": ["openclaw", "memory-palace", "android", "plugin"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "openclaw": {
    "extensions": ["./dist/index.js"]
  }
}
